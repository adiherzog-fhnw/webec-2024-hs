{% import "macros/nav" %}

<!DOCTYPE HTML>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <link rel="stylesheet" href="{{ href("/style.css") }}">
    <title>Contact List</title>
</head>
<div id="container">
    <header>
        <h1>Contact List</h1>
    </header>
    <nav>
        {{ nav("/", "Home", request) }}
        {{ nav("/contacts", "Contacts", request) }}
        {{ nav("/about", "About", request) }}
    </nav>
    <main>
        <h1>{% block title %}Contact List{% endblock %}</h1>

        {% block main %}{% endblock %}
    </main>
    <footer>
        sample solution week 5
    </footer>
</div>
</html>
